import { createComparisonPage } from "@/lib/comparison-page";

const { generateMetadata, default: Page } = createComparisonPage("eco-botanic-vs-medini");

export { generateMetadata };
export default Page;
