import { createComparisonPage } from "@/lib/comparison-page";

const { generateMetadata, default: Page } = createComparisonPage("forest-city-vs-rf-princess-cove");

export { generateMetadata };
export default Page;
