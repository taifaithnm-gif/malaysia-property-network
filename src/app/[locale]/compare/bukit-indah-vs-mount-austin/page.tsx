import { createComparisonPage } from "@/lib/comparison-page";

const { generateMetadata, default: Page } = createComparisonPage("bukit-indah-vs-mount-austin");

export { generateMetadata };
export default Page;
